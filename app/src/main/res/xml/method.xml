<?xml version="1.0" encoding="utf-8"?>
<input-method xmlns:android="http://schemas.android.com/apk/res/android"
              android:settingsActivity="dev.brgr.outspoke.settings.SettingsActivity">

    <!-- English subtype – default for all non-German locales (French, Spanish, etc.) -->
    <subtype
            android:label="@string/subtype_en"
            android:imeSubtypeLocale="en_US"
            android:imeSubtypeMode="voice"
            android:isDefault="true"/>

    <!-- German subtype – selected when device locale is German -->
    <subtype
            android:label="@string/subtype_de"
            android:imeSubtypeLocale="de_DE"
            android:imeSubtypeMode="voice"/>

</input-method>

